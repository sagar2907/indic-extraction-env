# Copy to .env and fill in. .env is gitignored and must never be committed.
# Only needed for live evaluation; the full test suite runs offline without keys.

# https://console.groq.com  -- free tier, no card
GROQ_API_KEY=

# https://aistudio.google.com  -- free tier, no card
GOOGLE_API_KEY=
